# Local development environment values (replaced remote credentials)
# Replace username/password/database as needed for your local Postgres instance

DATABASE_URL="postgresql://postgres:password@localhost:5432/defaultdb"
DIRECT_URL="postgresql://postgres:password@localhost:5432/defaultdb?sslmode=disable"


# so we are using accelare so for that we have database url and another is direct url 
